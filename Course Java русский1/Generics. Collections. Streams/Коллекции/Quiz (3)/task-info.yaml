type: choice
is_multiple_choice: false
options:
- text: если a.equals(b), то a.hashCode() == b.hashCode()
- text: если a.hashCode() == b.hashCode(), то a.equals(b)
- text: если !a.equals(b), то a.hashCode() != b.hashCode()
- text: a.equals(b) тогда и только тогда, когда a.hashCode() == b.hashCode()
files:
- name: src/Task.java
  visible: true
  text: |
    // You can experiment here, it won’t be checked

    public class Task {
      public static void main(String[] args) {
        // put your code here
      }
    }
  learner_created: false
status: Unchecked
record: -1
custom_name: Quiz
local_check: false
